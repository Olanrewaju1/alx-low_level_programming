#include "main.h"

/**
 * rev_string - function to reverse a string
 * @s:char point var
 *
 * Return: nothing
 */

void rev_string(char *s)
{
	int i, j;

	for (i = 0; s[i] != '\0'; i++)
	{

	}

	for (j = i - 1; j >= 0; j--)
	{
	}
}
