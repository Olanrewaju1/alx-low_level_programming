#include "main.h"

/**
 * _islower - function to check if a character is lowercase
 *
 * Return: 1
 */

int _islower(int c)
{
	char i;

	if (i >= 'a' && i <= 'z')
	{
		return (1);
	}

	else
		return (0);
}
